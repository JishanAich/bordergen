---
description: Load the bordergen project context (Fourier-series folk-art border generator) and continue from it
---

Load the bordergen project context before doing anything else.

1. Read `CONTEXT.md` in the project root (`D:\CXA\bordergen\CONTEXT.md`) in full. It holds the idea, the
   motivation, the maths, the art background, what exists, its limits and the working preferences.
2. Skim `README.md` only if the task touches the existing demo (run instructions, demo script, file map).
3. Treat the context as the project brief. Keep to its preferences: new work in Python, and a one-line
   comment above every function. Do not overclaim (no trained model yet, motifs are approximations).
4. Reply with a 3 to 5 line summary of where the project stands, so the user can confirm you have the right picture.

Then handle the task below. If there is no task, just give the summary and ask what to work on next.

Task: $ARGUMENTS
